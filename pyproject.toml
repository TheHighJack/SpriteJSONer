[project]
name = "spritesheet-editor"
version = "1.0.0"
description = "Editor visuale per spritesheet JSON"
requires-python = ">=3.9"
dependencies = ["Pillow>=10.0.0"]

[project.scripts]
spritesheet-editor = "spritesheet_editor:main"